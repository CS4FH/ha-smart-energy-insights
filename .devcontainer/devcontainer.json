{
  "name": "Smart Energy Insights Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/python:3.13-bookworm",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "keesschollaart.vscode-home-assistant",
        "charliermarsh.ruff"
      ]
    }
  },
  "forwardPorts": [8123],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ffmpeg libpcap-dev libturbojpeg0 && pip3 install -r /workspaces/${localWorkspaceFolderBasename}/requirements-dev.txt && mkdir -p /home/vscode/.homeassistant/custom_components && ln -sf /workspaces/${localWorkspaceFolderBasename}/custom_components/* /home/vscode/.homeassistant/custom_components/",
  "remoteUser": "vscode"
}